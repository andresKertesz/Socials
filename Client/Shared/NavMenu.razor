@using Socials.Client.Client.Controllers
@inject ComponentController componentController

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Socials</a>
    </div>
</div>

<div class="@NavMenuCssClass" >
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Eventos
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="categorias">
                <span class="oi oi-grid-three-up" aria-hidden="true"></span> Categorias
            </NavLink>
        </div>
    </nav>
</div>

<style>
</style>

@code {
    [Parameter]
    public MainLayout mainLayout { get; set; }

    private bool collapseNavMenu = false;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : "";

    private void ToggleNavMenu()
    {
        componentController.ShowNavBar = false;
        mainLayout.RefreshState();

    }
}
